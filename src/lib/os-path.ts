/**
 * Internal operating system path prefix.
 *
 * Used for hiding internal routes from search engines.
 * Empty string means no special prefix.
 */
export const OS_BASE_PATH = process.env.OS_BASE_PATH ?? "/os";
